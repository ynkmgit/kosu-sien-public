services:
  app:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ../data:/app/data
    environment:
      - DATABASE_PATH=/app/data/app.db
